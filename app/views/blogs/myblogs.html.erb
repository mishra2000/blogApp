<ul>
  <% @articles.each do |article| %>
    <% if current_user.id === article.user_id %>
    <li>
      <%= link_to article.title, article %>
      <%# <%= article.body %>
      <%# <%= article.user.email%>
    </li>
    <%end%>
  <% end %>
</ul>
